{% extends 'profile/partial/profile-base.html.twig' %}

{% block main_content %}

    <div class="row">
      <div class="col-md-6">
        <table class="table table-striped-columns">
          <thead>
            <tr>
              <th>Name</th>
              <th>Total Playlists</th>
              <th>Total Tracks</th>
            </tr>
          </thead>
          <tbody>
            {% for provider, data in stats %}
              <tr>
              <tr>
                <td>{{ provider|capitalize }}</td>
                <td>{{ data['playlists_count'] }}</td>
                <td>{{ data['tracks_count'] }}</td>
              </tr>
            {% endfor %}
          </tbody>

        </table>
      </div>
      <div class="col-md-3">
        <div class="settings-wrapper__main-content_new-connection">
          <a class="btn btn-default" id="connections-modal" href="#" data-bs-toggle="modal" data-bs-target=".new-transfer-modal">New import</a>
        </div>
      </div>

    </div>

  {{ include('/profile/partial/new-import-modal.html.twig') }}


{% endblock %}